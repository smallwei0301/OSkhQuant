# 核心套件 (部署與後端服務共用)
pandas==2.3.1
numpy==2.3.2
matplotlib==3.10.0
mplcursors==0.6

# 系統監控
psutil==6.1.1

# 網路與資料存取
requests==2.32.4
holidays==0.69
schedule==1.2.2
typing-extensions==4.14.1
openpyxl==3.1.5
xlrd==2.0.1
tabulate==0.9.0

# MiniQMT 交易端依賴（需自行安裝 xtquant 套件）
# xtquant 官方並未在 PyPI 發佈套件，因此請依 README 指引，
# 透過券商或官方提供的安裝包手動安裝對應版本後再啟動交易功能。
SQLAlchemy==2.0.36
boto3==1.35.54
oss2==2.18.1
clickhouse-connect==0.6.19

# 背景任務與排程
celery==5.4.0
redis==5.0.4
apscheduler==3.10.4
psycopg[binary]==3.2.1

# PyQt5 與其他桌面 GUI 依賴已移至 requirements-gui.txt，
# 以避免在無圖形介面的環境（例如 Netlify）部署時安裝失敗。
